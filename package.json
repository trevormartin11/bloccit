{
  "name": "flipcrm",
  "version": "1.0.0",
  "private": true,
  "description": "FlipCRM — a lightweight property pipeline CRM for real estate investors.",
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
